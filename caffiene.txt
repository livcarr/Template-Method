abstract class CaffieneBeverage with subclasses Tea and Coffee
  boilWater() and pour() are the same, while brew() and addCondiments() differ
    all happen during the algorithm prepareRecipe() though, so they're the steps of that algorithm.
